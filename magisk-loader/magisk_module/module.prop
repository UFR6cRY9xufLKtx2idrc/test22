id=${moduleId}
name=${api} - LSPosed
version=${versionName} (${versionCode})
versionCode=${versionCode}
author=${authorList}
description=Self Use Xposed Framework. Supports Android 8.1 ~ 15. ${requirement}.
updateJson=${updateJson}
